<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp">

    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="61dp"
        android:layout_marginTop="16dp"
        android:text="Enter the family ID if a family has been already created.
        If not, leave the space empty and clock on continue" />

    <EditText
        android:id="@+id/family_id_input"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Family ID"
        android:inputType="text"
        android:maxLines="1"
        android:layout_marginTop="16dp"/>

    <Button
        android:id="@+id/family_id_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Continue"
        android:layout_marginTop="16dp" />



</LinearLayout>
