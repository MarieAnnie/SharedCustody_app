package com.project.sharedcustodycalendar.utils

class FirebaseUtils {

    /*fun fetchFamilyData(token: String, onComplete: (FamilyData?) -> Unit) {
    val db = FirebaseFirestore.getInstance()
    db.collection("families").document(token).get()
        .addOnSuccessListener { doc ->
            if (doc != null && doc.exists()) {
                val data = doc.toObject(FamilyData::class.java)
                onComplete(data)
            } else {
                onComplete(null)
            }
        }
        .addOnFailureListener {
            onComplete(null)
        }
}*/

}